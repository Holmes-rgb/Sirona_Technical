{
	"name": "sirona-technical",
	"private": true,
	"description": "Monorepo root. Runs the Django API and the SvelteKit frontend together.",
	"scripts": {
		"predev": "npm install --no-audit --no-fund",
		"dev": "concurrently --names \"django,svelte\" --prefix-colors \"green,magenta\" --kill-others \"npm:dev:backend\" \"npm:dev:frontend\"",
		"dev:backend": "cd backend && uv run python manage.py dev",
		"dev:frontend": "cd frontend && npm run dev",
		"test": "npm run test:backend && npm run test:frontend",
		"test:backend": "cd backend && uv run pytest",
		"test:frontend": "cd frontend && npm run test"
	},
	"devDependencies": {
		"concurrently": "^10.0.5"
	}
}
